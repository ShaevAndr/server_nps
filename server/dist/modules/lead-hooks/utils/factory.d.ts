import { AmoLead, User, CustomField } from "../../amo-api/types/amo-api.types";
import { Company } from "../../company/core/company.entity";
import { Contact } from "../../contact/core/contact.entity";
import { Lead } from "../core/lead.entity";
import { Responsible } from "../../resonsible/core/responsible.entities";
import { Field } from "../../custom-fields/core/custom-field.entities";
export declare const companyFactory: (accountId: number, entityId: number, name: string) => Company;
export declare const contactFactory: (accountId: number, entityId: number, name: string) => Contact;
export declare const leadFactory: (amoLead: AmoLead) => Lead;
export declare const responsibleFactory: (amoResponsible: User, accountId: number) => Responsible;
export declare const fieldFactory: (accountId: number, field: CustomField) => Field;
